# dotaikit Homebrew Tap

Homebrew formulas for dotaikit tools.

## Install

```sh
brew tap dotaikit/tap
brew install acm
```

## Upgrade

```sh
brew update
brew upgrade acm
```

Formulas live in `Formula/`. The `acm` formula is updated automatically by the `dotaikit/acm` release workflow.
